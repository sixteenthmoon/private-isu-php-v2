name: private-isu
services:
  nginx:
    image: nginx:1.30
    restart: unless-stopped
    depends_on:
      - app
    volumes:
      - ./etc/nginx/conf.d:/etc/nginx/conf.d
      - ./public:/public
    ports:
      - "80:80"
    networks:
      - my_network

  app:
    build:
      # Go実装は golang/ PHP実装は php/ Python実装は python/ Node.js実装は node/ に変更
      context: php/
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - mysql
      - memcached
    environment:
      ISUCONP_DB_HOST: mysql
      ISUCONP_DB_PORT: 3306
      ISUCONP_DB_SOCKET: /var/run/mysqld/mysqld.sock
      ISUCONP_DB_USER: root
      ISUCONP_DB_PASSWORD: root
      ISUCONP_DB_NAME: isuconp
      ISUCONP_MEMCACHED_ADDRESS: memcached:11211
      ISUCONP_MEMCACHED_SOCKET: /var/run/memcached/memcached.sock
    networks:
      - my_network
    volumes:
      - ./public:/home/public
      - mysql-run:/var/run/mysqld
      - memcached-run:/var/run/memcached
    init: true
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 1g

  mysql:
    image: mysql:9.7
    restart: unless-stopped
    # ベンチ環境はinitializeで毎回既知状態へリセットされるため、mysqld crash時に
    # 最大1秒分のcommit済みトランザクションを失うリスクを許容しcommit毎fsyncを避ける。
    command: --innodb-flush-log-at-trx-commit=2
    environment:
      #- "TZ=Asia/Tokyo"
      - "MYSQL_ROOT_HOST=%"
      - "MYSQL_ROOT_PASSWORD=root"
    volumes:
      - mysql:/var/lib/mysql
      - mysql-run:/var/run/mysqld
      - ./sql:/docker-entrypoint-initdb.d
    ports:
      - "3306:3306"
    networks:
      - my_network
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 1g

  memcached:
    image: memcached:1.6
    restart: unless-stopped
    # appとunix socketで通信し、docker bridge経由のTCP接続overheadを避ける。
    command: -s /var/run/memcached/memcached.sock -a 766
    volumes:
      - memcached-run:/var/run/memcached
    networks:
      - my_network

volumes:
  mysql:
  mysql-run:
  memcached-run:

networks:
  my_network:
